stages:
  - lint
eslint:
  stage: lint
  variables:
    NVM_DIR: "$HOME/.nvm"
    NODE_VERSION: 14.17.0
  script:
    - curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash
    - \. "$NVM_DIR/nvm.sh"
    - npm install
    - npm run lint
